export function initSite() {
const WORDS = [   "Flowsheet PFD","BPMN 2.0","EAP / WBS","PM Canvas","Ishikawa 6M","5W2H",   "Gantt CPM","Kanban","Scrum Sprint","PDCA Circular","Script Engine",   "14 Temas","GNU GPLv3","PyQt5","Balanço de Massa","Caminho Crítico",   "Story Points","QGraphicsScene","Neo-Brutalism","Python 3.8+" ];  const track = document.getElementById('ticker'); const seg = [...WORDS,...WORDS].map(w=>`<span class="t-item">${w}<span class="t-dot"></span></span>`).join(''); track.innerHTML = seg+seg;  /* reveal */ const ro = new IntersectionObserver(es=>{   es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');ro.unobserve(e.target)}}) },{threshold:.1}); document.querySelectorAll('.rv').forEach(el=>ro.observe(el));  /* nav active */ const links = document.querySelectorAll('.nav-links a'); const so = new IntersectionObserver(es=>{   es.forEach(e=>{     if(e.isIntersecting){       links.forEach(a=>a.classList.remove('on'));       const m = document.querySelector(`.nav-links a[href="#${e.target.id}"]`);       if(m) m.classList.add('on');     }   }) },{threshold:.3}); document.querySelectorAll('section[id]').forEach(s=>so.observe(s));
}
